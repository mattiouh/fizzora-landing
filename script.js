const cart = []; const euro = n => new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR'}).format(n);
const drawer = document.querySelector('#cart'), overlay = document.querySelector('#overlay'), toast = document.querySelector('#toast');
function renderCart(){const items=document.querySelector('#cartItems'); const count=cart.reduce((a,x)=>a+x.qty,0), total=cart.reduce((a,x)=>a+x.qty*x.price,0); document.querySelector('#cartCount').textContent=count; document.querySelector('#cartQty').textContent=count; document.querySelector('#cartTotal').textContent=euro(total); items.innerHTML=cart.length?cart.map((x,i)=>`<div class="cart-line"><div class="cart-dot"></div><div><strong>${x.name}</strong><small>${euro(x.price)}</small></div><div class="stepper"><button data-index="${i}" data-change="-1">−</button><span>${x.qty}</span><button data-index="${i}" data-change="1">+</button></div></div>`).join(''):'<p class="empty">Il tuo carrello è ancora troppo silenzioso.</p>'; document.querySelectorAll('.stepper button').forEach(b=>b.addEventListener('click',()=>{const x=cart[+b.dataset.index];x.qty+=+b.dataset.change;if(!x.qty)cart.splice(+b.dataset.index,1);renderCart()}));}
function openCart(){drawer.classList.add('open');overlay.classList.add('open');drawer.setAttribute('aria-hidden','false')}function closeCart(){drawer.classList.remove('open');overlay.classList.remove('open');drawer.setAttribute('aria-hidden','true')}
document.querySelectorAll('[data-flavor]').forEach(b=>b.addEventListener('click',()=>{let x=cart.find(x=>x.name===b.dataset.flavor);if(x)x.qty++;else cart.push({name:b.dataset.flavor,price:+b.dataset.price,qty:1});renderCart();toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}));
document.querySelector('#cartButton').onclick=openCart; document.querySelector('#closeCart').onclick=closeCart;overlay.onclick=closeCart;
document.querySelector('#newsletter').addEventListener('submit',e=>{e.preventDefault();e.target.innerHTML='<strong>Sei dentro. Benvenutə nel fizz ✦</strong>'}); document.querySelector('.checkout').onclick=()=>alert('Demo portfolio: qui inizierebbe il checkout.');
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
